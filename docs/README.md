# Mídia do projeto

- `interface-modo-demo.jpg` — a interface rodando em `?demo=1` (modelo simulado).
  Serve de referência do resultado esperado; **não substitui** a evidência da
  entrega.
- `demo.gif` — **falta gravar.** É o print/GIF exigido pelo enunciado. Grave com
  o modelo REAL: mostre um objeto para a câmera, a barra de confiança subindo e
  a lixeira correta abrindo sozinha. Sugestões de ferramenta: Peek / Kooha
  (Linux), ScreenToGif (Windows), Kap (macOS). Deixe o arquivo abaixo de ~8 MB.
